import type { NextRequest } from "next/server";
import { sourceQuerySchema } from "@/features/sources/schemas/source-query";
import { getPublicSources } from "@/features/sources/server/get-public-sources";
export const runtime="nodejs"; export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const params=request.nextUrl.searchParams;const parsed=sourceQuerySchema.safeParse({q:params.get("q")??undefined,publisher:params.get("publisher")??undefined,type:params.get("type")??undefined,module:params.get("module")??undefined,page:params.get("page")??undefined,limit:params.get("limit")??undefined});if(!parsed.success)return Response.json({success:false,error:{code:"INVALID_QUERY",message:"Parameter katalog sumber tidak valid.",details:parsed.error.flatten().fieldErrors}},{status:400,headers:{"Cache-Control":"no-store"}});try{return Response.json({success:true,data:await getPublicSources(parsed.data)},{headers:{"Cache-Control":"public, s-maxage=300, stale-while-revalidate=600"}})}catch{console.error("Public source catalog failed.");return Response.json({success:false,error:{code:"INTERNAL_SERVER_ERROR",message:"Katalog sumber belum dapat dimuat."}},{status:500,headers:{"Cache-Control":"no-store"}})}}
